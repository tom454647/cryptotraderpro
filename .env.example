# Root-level .env.example — copied to .env for docker compose.
# App-specific envs live in apps/api/.env.example and apps/web/.env.example.

# Used by docker-compose (Postgres container init).
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=cryptotrader
